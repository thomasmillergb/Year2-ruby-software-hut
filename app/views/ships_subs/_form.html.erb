<%= form_for(@ships_sub) do |f| %>
  <% if @ships_sub.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ships_sub.errors.count, "error") %> prohibited this ships_sub from being saved:</h2>

      <ul>
      <% @ships_sub.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
