<link rel="stylesheet" type="text/css" media="all" href="assets/stylesheets/hideshow.css">

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Start</th>
      <th>Deadline</th>
      <th>Status</th>
      <th>Funder</th>
      <th>Awarded To</th>
      <th>Grant Code</th>
      <th>Final Report</th>
      <th></th>
    </tr>
  </thead>

  <tbody>
   
      
    <% @grants.each do |grant| %>
      
      <tr>
        
        <td><%= grant.name %></td>
        <td><%= grant.start %></td>
        <td><%= grant.deadline %></td>
        <td><%= grant.status  %></td>
	<td><%= grant.funder_id %></td>
        <td><%= grant.code %></td>        
        <td><%= grant.awarded_to %></td>
	<td><%= grant.finalreport %></td>
        <td>

<label>Change Status:</label> <input class="form-checkbox" id="<%=grant.id%>" type="checkbox">


<div class="<%=grant.id%>">




<%= form_for (Grant.find(grant.id))  do |f| %>

<%=  f.select(:status , options_for_select([['Allocated',1],['Complete',2]])) %>
    <%= f.submit %>
sdfsdf

<%end%>

</div>


	
        <td><%= link_to 'List Tasks', tasks_path(:project_id => Project.find_by_grant_id(grant)) %>
        <td><%= link_to 'Destroy', grant, method: :delete, data: { confirm: 'Are you sure?' } %></td>
     
 </tr>
     
    <% end %>
    
  
  </tbody>
</table>

<br>

<%= link_to 'New Grant', new_grant_path %>
