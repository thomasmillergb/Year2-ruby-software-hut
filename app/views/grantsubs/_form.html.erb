<%= form_for(@grantsub) do |f| %>
  <% if @grantsub.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@grantsub.errors.count, "error") %> prohibited this grantsub from being saved:</h2>

      <ul>
      <% @grantsub.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
