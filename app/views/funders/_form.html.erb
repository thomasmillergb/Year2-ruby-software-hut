<%= form_for(@funder) do |f| %>
  <% if @funder.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@funder.errors.count, "error") %> prohibited this funder from being saved:</h2>

      <ul>
      <% @funder.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    Type Name<br>
    <%= f.select :type_id, @options%>
    <%= link_to 'New Type', new_type_path %>

  </div>
  <div class="field">
    <%= f.label :phone %><br>
    <%= f.text_field :phone %>
  </div>
  <div class="field">
    <%= f.label :email %><br>
    <%= f.text_field :email %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
