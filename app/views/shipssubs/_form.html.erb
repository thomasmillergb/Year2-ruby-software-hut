<%= form_for(@shipssub) do |f| %>
  <% if @shipssub.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@shipssub.errors.count, "error") %> prohibited this shipssub from being saved:</h2>

      <ul>
      <% @shipssub.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
