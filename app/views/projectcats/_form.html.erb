<%= form_for(@projectcat) do |f| %>
  <% if @projectcat.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@projectcat.errors.count, "error") %> prohibited this projectcat from being saved:</h2>

      <ul>
      <% @projectcat.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
